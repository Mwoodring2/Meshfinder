# 📂 Project Folder Template — ModelFinder / 3D ML Archive (Patched)

This template is updated to reflect **true Phase 1 capabilities** and corrected docs alignment.

```plaintext
ModelFinder_Project/
│
├── data/                      # 3D assets & outputs
│   ├── raw/                   # Original STL, OBJ, ZTL, FBX files (ZTL placeholders only)
│   ├── glb/                   # Normalized GLB exports (supported now)
│   ├── posters/               # Preview renders (optional)
│   └── metrics/               # JSON/CSV of computed mesh stats
│
├── db/                        # Index storage
│   ├── modelfinder.db         # SQLite database (metadata, metrics, embeddings)
│   └── faiss.index            # FAISS similarity index (optional, future)
│
├── src/                       # Source code
│   ├── indexer/               # Scanning + metrics + converters (current core)
│   ├── embedder/              # Learned embeddings (future ML)
│   ├── qa/                    # Repair + validation (future)
│   ├── ui/                    # PyQt UI (future)
│   └── utils/                 # Helpers (logging, config)
│
├── scripts/                   # CLI utilities & build scripts
│   ├── build_exe.bat          # Windows: one-click PyInstaller build
│   ├── build_run.ps1          # PowerShell: venv setup + scan + build
│   ├── scan_folder.py         # Run indexing pipeline
│   └── search_cli.py          # Similarity + metadata search CLI
│
├── tests/                     # Unit tests & sample assets (future)
│
├── docs/                      # Documentation
│   ├── roadmap.md             # Development phases
│   ├── architecture.md        # Current vs future system layout
│   └── setup_guide.md         # Install & usage guide (with PS tips)
│
├── requirements-core.txt      # Core deps: numpy, faiss-cpu, trimesh, pygltflib, pillow, tqdm, pyrender, PyOpenGL
├── requirements-ui.txt        # (Later) PyQt6, PyQt6-tools
├── requirements-ml.txt        # (Later) torch, torchvision, transformers, open3d/kaolin
├── README.md                  # Project overview & quickstart
└── .gitignore                 # Git hygiene (skip venv, build, db, data outputs)
```

---

### 🔑 Patch Notes

**1. Embeddings**  
- Corrected: deterministic **10D geometric embeddings** (bbox, tris, area, watertight, aspect ratios).  
- Replaceable later with **learned ML embeddings**.

**2. Database schema**  
- Canonical: `assets` table (path, ext, size, hash, metrics) + `embeddings` table (asset_id, vec).  
- No `filename`/`placeholder random vectors`. Docs updated.

**3. Requirements split**  
- `requirements-core.txt`: minimal, safe for Phase 1.  
- `requirements-ui.txt`: only when UI starts.  
- `requirements-ml.txt`: only when ML training starts.  
- Removed `sqlite3` (stdlib), heavy ML deps from Phase 1.

**4. Supported formats**  
- **Now**: `.obj`, `.stl` (ZTL recorded as placeholder).  
- **Exports**: `.glb`.  
- **Planned**: FBX, PLY, 3DS, DAE, ZTL → via Assimp or ZBrush bridge.

**5. FAISS index**  
- Current CLI rebuilds FAISS in-memory from SQLite.  
- Disk-based `faiss.index` is **optional future optimization** (flag TBD).

**6. Entrypoints**  
- Clarified: `src/indexer/modelfinder_indexer.py` is **engine**.  
- CLI tools (`scan_folder.py`, `search_cli.py`) are the **user-facing entrypoints**.

**7. Windows notes**  
- Execution policy: recommend `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` before running PowerShell scripts.  
- Poster renders (`pyrender`) may fail on headless setups; optional.

---

✅ This now matches **actual working code** in Phase 1 and leaves clean paths open for Phase 2–4 (intelligence, advanced, ecosystem).
